services:

  caddy:
    image: caddy:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./sites:/srv
      - ./data:/data
      - ./config:/config
    ports:
      - "80:80"
      - "443:443"
    networks:
      - web

networks:
  web:
    external: true
